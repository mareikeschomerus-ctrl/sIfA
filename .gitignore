# ── OS metadata ────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.textClipping

# ── Editor / backup files ──────────────────────────────────────
*.bak
*~
*.swp
.vscode/
.idea/

# ── Python build / cache ───────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/

# ── Project-local reference material, not for the repo ─────────
_reference/

# ── Historical builds — kept locally for reference, not shipped
archive/

# Empty leftover from the pre-flatten layout — delete in Finder when you get a chance
example-data.sifa.json
orig-data.sifa.json
